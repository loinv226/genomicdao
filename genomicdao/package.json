{
  "name": "genomicdao",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:node": "npx hardhat node",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run --network default scripts/deploy.js",
    "start:console": "npx hardhat console --network default",
    "verify": "npx hardhat verify --network default --constructor-args arguments.js DEPLOYED_CONTRACT_ADDRESS",
    "test": "npx hardhat test --network default"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "chai": "^4.4.1",
    "dotenv": "^16.4.0",
    "ethers": "^6.10.0",
    "hardhat": "^2.17.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1"
  }
}
